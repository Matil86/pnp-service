management:
  endpoints:
    web:
      base-path: /
      path-mapping:
        health: health
      exposure:
        include:
          - "health"
server:
  port: ${SERVER_PORT:0}
spring:
  profiles:
    include: rabbitmq,h2-database,logging,genefunk-crb-locale, genefunk-crb,oauth