export class Attribute{
  value:number;
  max:number;
  modifier:number;


  constructor(value: number, max: number, modifier: number) {
    this.value = value;
    this.max = max;
    this.modifier = modifier;
  }
}
